# Autopilot 与 Agent Swarm 协议

## 持续执行循环

主控在 Intake 后运行：

```powershell
python -m modelharness.autopilot next
```

它返回当前阶段、缺失证据、审核状态、建议并行角色和文件所有权。主控执行返回的
任务后再次调用 `autopilot next`。这个循环持续到 S6 完成，不以单个阶段完成为终点。

```text
ORIENT → DECOMPOSE → PARALLEL WORK → INTEGRATE
   → VERIFY → GATE → NEXT STAGE
                ↑         |
                └─ REPAIR ┘
```

## 何时开 subagent

满足以下全部条件时开：

- 子任务可独立完成；
- 输入和验收标准明确；
- 写入文件集合互不重叠；
- 并行收益大于沟通成本。

典型用途：候选模型竞争、文献/数据并行调查、独立基线、UQ 分片、敏感性扫描、
无状态数值审核、论文复核。

不应用于：同一核心文件的并行编辑、尚未明确接口的强依赖任务、把主控应做的最终
裁决整体外包。

## Subagent 合同

每个任务必须声明：

- 只读输入；
- 唯一写入范围；
- 预期机器产物；
- 验收命令；
- 预算/种子/分片范围；
- 完成或失败返回格式。

并行工作者完成后，主控先检查文件和测试，再整合；不能只相信自然语言汇报。

## 审核循环

审核者必须冷启动，不继承生成者的讨论，只读取题面、正式规格、产物、检查和证据图。

```text
REVIEW → REJECT? → revoke/invalidate → FIX → COLD REVIEW
       → APPROVE → GATE
```

同一阻断连续三轮无法消除才停止并请求用户决策。普通失败、测试红灯或一次 REJECT
不是停止条件。

## 长计算循环

- 先 smoke，再小规模，再正式规模；
- 按互不重叠的种子或参数区间分片；
- 每片原子写 checkpoint；
- worker 失败只恢复其负责范围；
- 全部分片完成后单独汇总；
- 监控无错误时不打扰用户，有异常时自动恢复；
- 不因一次进程退出重跑已完成分片。

## 唯一允许停止的条件

1. S6 已通过并完成最终完整性审计；
2. 缺少只有用户能提供的文件、凭据或重大策略选择；
3. 同一阻断已连续三轮复现且无安全的内部替代方案；
4. 下一步需要超出用户授权的外部写入或高风险操作。

“完成 S0”“某个 subagent 完成”“等待审核”“一次测试失败”都不是停止条件。

