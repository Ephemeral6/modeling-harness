# Contributing

欢迎提交 issue、设计讨论和 pull request。

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 保持核心包仅依赖 Python 标准库；新增依赖须说明必要性。
3. 为证据图、门禁或叙事审计行为添加回归测试。
4. 运行 `python -m pytest -q` 和 `python -m compileall -q modelharness`。
5. PR 中说明变更影响的阶段、证据类型和失效行为。

## 设计原则

- 草稿不是真相；只有通过验证的证据才能支撑决策。
- 验证者必须与产物生成者职责隔离。
- 上游失效必须级联撤销下游结论。
- 对不显著、不可辨识或稳健集为空的结果诚实降级。
- 不提交私有数据、API 密钥、账号信息或竞赛受限材料。
